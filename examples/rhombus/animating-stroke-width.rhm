#lang rhombus

// SCENE-AS Rhombus bridge example.
// The Racket module exports the semantic scene constructor used by the
// command-line movie example.

import:
  lib("animating-stroke-width.rkt")

#{make-demo-scene}()
