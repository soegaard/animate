#lang rhombus

// SCENE-AF Rhombus bridge example.
// The Racket module exports the same semantic scene constructor used by the
// command-line movie example.

import:
  lib("open-compound-morph-correspondence.rkt")

#{make-demo-scene}()
