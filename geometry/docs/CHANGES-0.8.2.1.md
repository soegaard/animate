# Changes in v0.8.2.1

- Fixed the `cond`/`dynamic-wind` delimiter sequence in `geometry/render.rkt`.
- Fixed the closing delimiter sequence in the new static-frame reuse integration test.
- Parsed all 69 Racket modules successfully with Racket CS 9.3.0.8 before packaging.
- No intended change to the static-frame reuse algorithm or public API.
